<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/home/index">Home</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><a href="/dishes">Todos os pratos</a></li>
        <li><a href="/ingredients">Ingredientes</a></li>
      </ul>
      <form class="navbar-form navbar-left">
        <div class="form-group">
          <%= form_tag('/restaurants', method: 'get') do %>

            <input type="text" class="form-control " placeholder="Buscar categorias" <%= text_field_tag(:term) %>
            </div>

            <button type="submit" class="btn btn-default">Buscar</button>
          <% end %>

          </form>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>

<p id="notice"><%= notice %></p>

<div class="container-fluid">
<h2>Todos os pratos cadastrados</h2>
<div class="container-fluid">
<table class="table table-striped">
  <thead>
    <tr>
      <th>Nome</th>
      <th>Preço</th>
      <th>Tempo de preparo em minutos</th>
      <th>Categoria</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @dishes.each do |dish| %>
      <tr>
        <td><%= dish.name %></td>
        <td><%= dish.price %></td>
        <td><%= dish.time_prep %></td>
        <td><%= dish.category %></td>
        <td><%= link_to 'Visualizar', dish, :class => "btn btn-info" %></td>

        <td><%= link_to 'Apagar', dish, method: :delete, data: { confirm: 'Deseja realmente apagar?' }, :class => "btn btn-danger" %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Cadastrar novo prato', new_dish_path, :class => "btn btn-success btn-lg" %>
</div>
</div>