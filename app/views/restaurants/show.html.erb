<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/home/index">Home</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><a href="/dishes">Todos os pratos</a></li>
        <li><a href="/ingredients">Ingredientes</a></li>
      </ul>
      <form class="navbar-form navbar-left">
        <div class="form-group">
          <%= form_tag('/restaurants', method: 'get') do %>

            <input type="text" class="form-control " placeholder="Buscar categorias" <%= text_field_tag(:term) %>
            </div>

            <button type="submit" class="btn btn-default">Buscar</button>
          <% end %>

          </form>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>

<p id="notice"><%= notice %></p>

<div class="container-fluid">
<div class="panel panel-default">
  <div class="panel-body">
    <strong>Informações do restaurante:</strong>
  </div>
  <div class="panel-footer">Nome: <strong><%= @restaurant.name %></strong></div>
  <div class="panel-footer">Telefone: <strong><%= @restaurant.telephone %></strong></div>
  <div class="panel-footer">Endereço: <strong><%= @restaurant.address %></strong></div>
</div>

  <div class="panel panel-default">
    <div class="panel-body">
      <h4><strong>Pratos servidos nesse restaurante</strong></h4>
    </div>
<div>
  <table class="table table-striped">
    <thead>
    <tr>
      <th>Nome</th>
      <th>Preço</th>
      <th>Tempo de preparo</th>
      <th>Categoria</th>
      <th colspan="4"></th>
    </tr>
    </thead>

    <tbody>
    <% @dishes.order("name").each do |dish|%>
      <tr>
        <td><%= dish.name %></td>
        <td><%= dish.price %></td>
        <td><%= dish.time_prep %></td>
        <td><%= dish.category %></td>
        <td><%= link_to 'Visualizar', dish, :class => "btn btn-info" %></td>
        <td><%= link_to 'Editar', edit_dish_path(dish), :class => "btn btn-warning"%></td>
        <td><%= link_to 'Apagar', dish, method: :delete, data: { confirm: 'Você tem certeza?' }, :class => "btn btn-danger" %></td>
      </tr>
    <% end %>
    </tbody>
  </table>
</div>
  </div>
  <div>
    <%= link_to 'Voltar', restaurants_path, :class => "btn btn-info" %> |
  <%= link_to 'Editar', edit_restaurant_path(@restaurant), :class => "btn btn-warning" %>

  </div>

  </div>


